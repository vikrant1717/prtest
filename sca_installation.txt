Here are simple, clean steps you can paste into a Confluence page:

---

## Install Syft and Grype on Windows using Chocolatey

### Prerequisite

* Open **PowerShell as Administrator**

---

### Step 1: Install Chocolatey (if not already installed)

Run the following command:

```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.SecurityProtocolType]::Tls12
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
```

---

### Step 2: Install Syft

```powershell
choco install syft -y
```

---

### Step 3: Install Grype

```powershell
choco install grype -y
```

---

### Step 4: Verify Installation

```powershell
syft version
grype version
```

---

### Expected Outcome

* Syft and Grype should be installed successfully
* Version details should be displayed in the console

---

### Notes

* Restart PowerShell if commands are not recognized
* Ensure internet connectivity during installation

---

If you want, I can also format this with headings/macros (like Confluence panels, code blocks, etc.).
